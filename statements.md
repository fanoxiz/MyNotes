---
title: "Сложности вычислений, 2026, утверждения"
author: "@witzberg"
date: "2026-05-29"
---

---

# 1. $\mathbf{P \subset NP \subset EXP}$

> $\mathbf{P \subset NP \subset EXP}$

$\triangle$

**$\mathbf{P \subset NP}$:**  
Если язык $L \in \mathbf{P}$, существует полиномиально вычислимая функция-решатель. Определим верификатор для $\mathbf{NP}$, который просто игнорирует сертификат и запускает решатель из $\mathbf{P}$. Так как решатель работает полиномиальное время, верификатор тоже. Следовательно, $L \in \mathbf{NP}$

**$\mathbf{NP \subset EXP}$:**  
Если язык $L \in \mathbf{NP}$, то существует полином $p(n)$ и верификатор $V(x, s)$, где $|s| \le p(|x|)$. Чтобы решить задачу детерминированно, можно перебрать все возможные строки $s$ длины до $p(n)$ (которых $2^{p(n)+1}$ штук). На каждую проверку $V(x, s)$ тратится $\text{poly}(n)$ времени. Общее время работы ограничено $\text{poly}(n) \cdot 2^{p(n)} = O(2^{n^c})$ для некоторого $c$. Следовательно, $L \in \mathbf{EXP}$

---

# 2. Транзитивность сводимости по Карпу

> Сводимость по Карпу транзитивна

$\triangle$

Пусть функции сведения $f: A \to B$ и $g: B \to C$ вычислимы за время $p(|x|)$ и $q(|x|)$, где $p, q$ - полиномы. Тогда сведение $A$ к $C$ - это композиция $h(x) = g(f(x))$.
- **Корректность:** $x \in A \iff f(x) \in B \iff g(f(x)) \in C$
- **Время работы:** $|f(x)| \le p(|x|)$. Тогда для вычисления $g(x)$ верно: $q(|f(x)|) \le q(p(|x|))$. Сумма времени работы обеих функций, $p(|x|) + q(p(|x|))$, ограничена полиномом от $|x|$. Следовательно, композиция $h$ полиномиально вычислима.

---

# 3. Замкнутость классов относительно сводимости по Карпу

> Если $A \le_p B$ и $B \in \{\mathbf{P}, \mathbf{NP}, \mathbf{PSPACE}, \mathbf{\Sigma_k^p}, \mathbf{\Pi_k^p}\}$, то $A$ лежит в том же классе

$\triangle$

Почему не выходим за пределы классов:
1. **По времени:** Композиция двух полиномов полином
2. **По длине входа:** Так как $f$ работает за полиномиальное время $p(n)$, длина выхода $|f(x)|$ физически не может превысить число шагов, то есть $|f(x)| \le p(n)$
3. **По памяти:** Память для запуска решателя $B$ на входе $f(x)$ ограничена полиномом $q(|f(x)|) \le q(p(n))$, что является полиномом от $n$

---

# 4. Замкнутость $\mathbf{P}$ и $\mathbf{NP}$ относительно теоретико-множественных операций

- **Относительно $\bigcap$ и $\bigcup$:**  
Для $\mathbf{P}$ достаточно запустить полиномиальные алгоритмы друг за другом.  
Для $\mathbf{NP}$ - недетерминированно выбрать нужную ветвь для объединения или угадать сертификаты для обеих задач для пересечения.

- **Относительно дополнения:**  
Для $\mathbf{P}$ достаточно инвертировать ответ полиномиального детерминированного алгоритма.  
Для $\mathbf{NP}$ вопрос равносилен равенству $\mathbf{NP} = \mathbf{coNP}$. Считается, что $\mathbf{NP}$ не замкнут относительно дополнения

---

# 5. Принадлежность к $\mathbf{NP}$ классических задач 

> Задачи о выполнимости (SAT), 3-раскраске, клике, гамильтоновом пути и рюкзаке лежат в классе $\mathbf{NP}$

$\triangle$

Чтобы показать, что задача лежит в $\mathbf{NP}$, нужно дать сертификат, длина которого ограничена полиномом, и полиномиальный алгоритм верификатора

- **Выполнимость формулы ($\text{SAT}$):**  
  *Сертификат:* набор значений переменных (длина равна числу переменных)  
  *Верификатор:* подставляет значения в формулу и вычисляет ее истинность
- **3-раскраска ($\text{3COL}$):**  
  *Сертификат:* массив цветов ($1$, $2$ или $3$) для каждой вершины  
  *Верификатор:* проходит по всем ребрам графа и проверяет, что цвета на концах каждого ребра различны (время $O(|E|)$)
- **Клика ($\text{CLIQUE}$):**  
  *Сертификат:* список из $k$ вершин  
  *Верификатор:* проверяет, что для любой пары вершин из этого списка в графе существует ребро (время $O(k^2)$)
- **Гамильтонов путь ($\text{HAMPATH}$):**  
  *Сертификат:* последовательность из $n$ вершин ($n = |V|$)  
  *Верификатор:* проверяет, что все вершины в списке уникальны, и между любыми двумя соседними в списке вершинами есть ребро в графе (время $O(n)$)
- **Рюкзак ($\text{KNAPSACK}$):**  
  *Сертификат:* список индексов, указывающий какие предметы берутся в рюкзак  
  *Верификатор:* суммирует веса выбранных предметов, сравнивает с максимумом, суммирует их стоимости, сравнивает с минимумо

---

# 6. Сводимость задачи о 3-раскраске к задаче о выполнимости

> $3\text{COL} \le_p \text{SAT}$

$\triangle$

Нужно по графу $G=(V,E)$ за полиномиальное время построить булеву формулу $\Phi$, которая выполнима тогда и только тогда, когда граф можно раскрасить в 3 цвета  
Для каждой вершины $v \in V$ заводмм две булевы переменные $p_v^1$ и $p_v^2$, которые кодируют цвет (например `01, 10, 11`)

Искомая формула $\Phi$ - конъюнкцию двух типов условий:

- **Каждая вершина имеет корректный цвет:**  
  $(p_v^1 \lor p_v^2)$ - чтобы не пропустить некорректный цвет `00`
- **Смежные вершины имеют разные цвета:**  
  Для каждого ребра $(u, v) \in E$ цвета вершин $u$ и $v$ должны отличаться хотя бы в одном бите:  
  $(p_u^1 \neq p_v^1) \lor (p_u^2 \neq p_v^2)$

Итоговая формула $\Phi$ - это конъюнкция всех этих скобок. Ее размер - $O(|V| + |E|)$

---

# 7. Сводимость задачи о выполнимости произвольной формулы к задаче о выполнимости 3-КНФ

> $\text{SAT} \le_p 3\text{SAT}$

$\triangle$

Любую булеву формулу $\Phi$ можно за полиномиальное время превратить в эквивалентную формулу $\Psi$ в формате $3$-КНФ. Алгоритм:

**1.**  
Представим формулу $\Phi$ в виде дерева синтаксического разбора, где листья - переменные, а внутренние узлы - логические операции $\land, \lor, \neg$

**2.**  
Для каждого внутреннего узла вводим новую булеву переменную. Теперь каждая логическая операция связывает максимум $3$ переменные  
Например, если узел $z$ является конъюнкцией узлов $x$ и $y$, записываем это как $z \leftrightarrow (x \land y)$

**3.**  
Выражение $z \leftrightarrow (x \land y)$ зависит от $3$ переменных, поэтому его таблица истинности имеет $8$ строк $\implies$ можно записать в виде КНФ из не более чем $8$ скобок, длина этой КНФ ограничена константой  
Короткие скобки, в которых $<3$ литералов, добиваются фиктивной переменной: $A \equiv (A \lor z) \land (A \lor \neg z)$  
Например, $z \leftrightarrow (x \lor y)$ в виде КНФ выглядит как $(z \lor \neg x) \land (z \lor \neg y) \land (\neg z \lor x \lor y)$

Итоговая формула $\Psi$ - конъюнкция мини-КНФ для каждого узла дерева **И** переменной, соответствующей корню дерева (условие того, что вся формула истинна). Время работы и длина выхода - $O(|\Phi|)$

---

# 8. Сводимость задач о клике, независимом множестве и вершинном покрытии друг к другу

> $\text{CLIQUE} \le_p \text{INDSET}$  
> $\text{INDSET} \le_p \text{CLIQUE}$  
> $\text{INDSET} \le_p \text{VERTEXCOVER}$  
> $\text{VERTEXCOVER} \le_p \text{INDSET}$  

где:  
$\text{CLIQUE} = \{ (G, k) \mid \text{в G есть клика размера } k \}$  
$\text{INDSET} = \{ (G, k) \mid \text{в G есть } k \text{ вершин, попарно не соединенные ребрами} \}$  
$\text{VERTEXCOVER} = \{ (G, k) \mid \text{в G есть } k \text{ вершин, что любое ребро задевается хоть одной из них} \}$

$\triangle$

**1. Сводимость $\text{CLIQUE}$ и $\text{INDSET}$ друг к другу:**  
$S \subset V$ - клика в $G$ $\iff$ $S$ - независимое множество в $\overline{G}$. Функция сведения:  
$f((G, k)) = (\overline{G}, k)$

**2. Сводимость $\text{INDSET}$ и $\text{VERTEXCOVER}$ друг к другу:**  
$S \subset V$ - независимое множество размера $k$ в $G$ $\iff$ $V \setminus S$ - вершинное покрытие с параметром $|V| - k$ в $G$. Функция сведения:  
$f((G, k)) = (G, |V| - k)$

---

# 9. Сводимость задачи о гамильтоновом пути к задаче о гамильтоновом цикле

> $\text{HAMPATH} \le_p \text{HAMCYCLE}$

где:  
$\text{HAMPATH} = \{(G, s, t) \mid \text{в G есть гамильтонов путь из s в t} \}$  
$\text{HAMCYCLE} = \{G \mid \text{в G есть гамильтонов цикл} \}$

$\triangle$

Функция сведения:  
$f(G = (V, E), s, t) = G', G' = (V \cup \{v_{new} \}, E \cup {(t, v_{new}), (v_{new}, s)})$


---

# 10. Дополнение к NP-полному языку является coNP-полным

> Если $L \in \mathbf{NPC}$, то $\overline{L} \in \mathbf{coNPC}$

$\triangle$

Нужно доказать, что:
- $\overline{L} \in \mathbf{coNP}$
- $\overline{L} \in \mathbf{coNPH}$

**1. Принадлежность к $\mathbf{coNP}$:**  
$L \in \mathbf{NPC} \implies L \in \mathbf{NP} \implies \overline{L} \in \mathbf{coNP}$

**2. Принадлежность к $\mathbf{coNPH}$:**  
Пусть $A \in \mathbf{coNP}$. Покажем, что $A \le_p \overline{L}$:

$A \in \mathbf{coNP} \implies \overline{A} \in \mathbf{NP} \implies$ существует полиномиальная функция $f$, сводящая $\overline{A}$ к $L$:
$$x \in \overline{A} \iff f(x) \in L$$
$$x \notin \overline{A} \iff f(x) \notin L$$
$$x \in A \iff f(x) \in \overline{L}$$

Т.е. та же самая $f$ сводит $A$ к $\overline{L}$. Так как $A$ - любой язык из $\mathbf{coNP}$, то $\overline{L} \in \mathbf{coNPH}$

$\implies \overline{L} \in \mathbf{coNPC}$

---

# 11. Вложенность классов полиномиальной иерархии

> $\forall k \ge 0:$  
> $$\mathbf{\Sigma_k^p} \subset \mathbf{\Sigma_{k+1}^p}$$
> $$\mathbf{\Sigma_k^p} \subset \mathbf{\Pi_{k+1}^p}$$
> И аналогично для $\mathbf{\Pi_k^p}$

$\triangle$

Все вложения доказываются по одному шаблону - добавление фиктивной переменной, от которой не зависит предикат. 
Пример для $\mathbf{\Sigma_k^p} \subset \mathbf{\Pi_{k+1}^p}$:

Пусть $A \in \mathbf{\Sigma_k^p}$. По определению:
$$x \in A \iff \exists y_1 \forall y_2 \exists y_3 \dots Q_k y_k: V(x, y_1, y_2, \dots, y_k) = 1$$

Рассмотрим верификатор $V'(x, y_0, y_1, \dots, y_k)$, который просто игнорирует свою переменную $y_0$:
$$V'(x, y_0, y_1, \dots, y_k) = V(x, y_1, \dots, y_k)$$

Так как $V'$ не зависит от $y_0$, вешаем на $y_0$ квантор $\forall$:
$$x \in A \iff \forall y_0 \exists y_1 \forall y_2 \dots Q_k y_k: V'(x, y_0, y_1, \dots, y_k) = 1$$

$\implies A \in \mathbf{\Pi_{k+1}^p}$

*Аналогично, если фиктивную переменную с квантором добавить не в начало, а в конец, получим сдвиг без смены типа класса*

---

# 12. Замкнутость (или незамкнутость) классов полиномиальной иерархии относительно теоретико-множественных операций

> Классы $\mathbf{\Sigma_k^p}$ и $\mathbf{\Pi_k^p}$ замкнуты относительно $\bigcap$ и $\bigcup$, но предположительно не замкнуты относительно дополнения

$\triangle$

**1. Незамкнутость относительно дополнения:**  
Отрицание формулы, начинающейся с $\exists$, по правилам де Моргана превращается в формулу, начинающуюся с $\forall$. Поэтому дополнение к языку из $\mathbf{\Sigma_k^p}$ всегда лежит в $\mathbf{\Pi_k^p}$. Если бы классы были замкнуты относительно дополнения, то выполнялось бы $\mathbf{\Sigma_k^p} = \mathbf{\Pi_k^p}$, что привело бы к коллапсу всей иерархии на уровне $k$. Считается, что иерархия не коллапсирует, значит, классы не замкнуты относительно дополнения

**2. Замкнутость относительно $\bigcap$ и $\bigcup$:**  
Пусть $A, B \in \mathbf{\Sigma_k^p}$. Для примера пусть $k=2$:  
$x \in A \iff \exists y_1 \forall y_2: V_A(x, y_1, y_2) = 1$  
$x \in B \iff \exists z_1 \forall z_2: V_B(x, z_1, z_2) = 1$

Тогда:  
$x \in A \cap B \iff \exists (y_1, z_1) \forall (y_2, z_2): V_A(x, y_1, y_2) \land V_B(x, z_1, z_2) = 1$

Мы получили формулу того же вида, предикат под кванторами вычисляется за полиномиальное время. Для $\bigcup$ и для $\mathbf{\Pi_k^p}$ аналогично

---

# 13. Классификация по уровням полиномиальной иерархии задач о минимальной эквивалентной формуле, об обобщенной рамсеевости, о кликовой раскраске

> Все эти задачи лежат в $\mathbf{\Sigma_2^p}$ или $\mathbf{\Pi_2^p}$

$\triangle$  

**1. Минимальная эквивалентная формула**  
*Условие:* Дана булева формула $\varphi$ и число $k$. Существует ли формула $\psi$ длины не более $k$, которая эквивалентна $\varphi$?  
*Логическая запись:* $\exists \psi \ (|\psi| \le k) \ \forall x: \psi(x) = \varphi(x)$  
*Классификация:* Формула имеет вид $\exists \forall$. Предикат проверки равенства полиномиален. Значит, задача лежит в $\mathbf{\Sigma_2^p}$

**2. Обобщенная рамсеевость**  
*Условие:* Даны три графа $F, G, H$. Верно ли, что для любой раскраски ребер $F$ в красный и синий цвета, найдется либо красный подграф, изоморфный $G$, либо синий подграф, изоморфный $H$?  
*Логическая запись:* $\forall$ раскрасок $\exists$ подграф $S: S \text{ - красный } \cong G \lor S \text{ - синий } \cong H$  
*Классификация:* Формула имеет вид $\forall \exists$. Значит, задача лежит в $\mathbf{\Pi_2^p}$

**3. Кликовая раскраска**  
*Условие:* Дан граф $G$ и число $k$. Существует ли раскраска вершин $G$ в $k$ цветов такая, что в графе нет ни одной одноцветной клики, которую нельзя расширить ("максимальная")?  
*Логическая запись:* $\exists$ раскраска $\forall$ множеств $X \subset V$: $\text{Если } X \text{ максимальная клика, то в } X \text{ есть хотя бы 2 цвета}$  
*Классификация:* Формула имеет вид $\exists \forall$. Значит, задача лежит в $\mathbf{\Sigma_2^p}$

---

# 14. Пример задачи на третьем или более высоком уровне полиномиальной иерархии

> $\text{HEREDITCLIQUECOL} \in \mathbf{\Pi_3^p}$

$\triangle$

(Из книги Мусатова) Возьмем задачу о кликовой раскраске из предыдущего пункта и усложним:

*Условие:* Верно ли, что для любого подграфа $H \subset G$, существует кликовая раскраска в $k$ цветов такая, что для любой максимальной клики $X$ в подграфе $H$ она не является одноцветной?  
*Логическая запись:* 
$\forall H \subset G \ \exists \text{ раскраска } \forall \text{ множеств } X \subset H: \text{Если } X \text{ макс. клика, то цвета не совпадают}$  
*Классификация:* Цепочка кванторов имеет вид $\forall \exists \forall$. Следовательно, задача лежит в $\mathbf{\Pi_3^p}$

---

# 15. Замкнутость (или незамкнутость) классов $\mathbf{L}$, $\mathbf{NL}$, $\mathbf{PSPACE}$ относительно теоретико-множественных операций

> Все три класса $\mathbf{L}$, $\mathbf{NL}$, $\mathbf{PSPACE}$ замкнуты относительно $\bigcap$, $\bigcup$ и дополнения

$\triangle$

**1. Замкнутость относительно $\bigcap$ и $\bigcup$:**  
Пусть языки $A$ и $B$ лежат в одном из этих классов.
- Для $\mathbf{L}$, $\mathbf{PSPACE}$:  
  Запускаем машину $M_A$ на входе $x$. Если нужен $M_B$, мы стираем рабочую ленту (**переиспользуем** память!) и запускаем $M_B$. Суммарная память равна $\max(S_A(n), S_B(n))$, что сохраняет асимптотику $\log n$ или $\text{poly}(n)$
- Для $\mathbf{NL}$:  
  Для объединения недетерминированно угадываем, какую машину запустить. Для пересечения запускаем $M_A$, и если она допускает, стираем память и запускаем $M_B$. Память снова не складывается, а берется как максимум

**2. Замкнутость относительно дополнения:**  
- Для $\mathbf{L}$, $\mathbf{PSPACE}$:  
  инвертируем ответ: $q_{accept} \leftrightarrow q_{reject}$
- Для $\mathbf{NL}$:  
  Теорема Иммермана-Селепченьи гласит, что $\mathbf{NL} = \mathbf{coNL}$

---

# 16. $\mathbf{PH} \subset \mathbf{PSPACE} \subset \mathbf{EXP}$

> $$\mathbf{PH} \subset \mathbf{PSPACE} \subset \mathbf{EXP}$$

$\triangle$

**1. $\mathbf{PH} \subset \mathbf{PSPACE}$:**  
Любая задача из $\mathbf{PH}$ - проверке истинности булевой формулы с $k$ чередующихся кванторов:
$\exists y_1 \forall y_2 \dots Q_k y_k: V(x, y_1, \dots, y_k) = 1$  
Поиск в глубину детерминированно вычисляет ее. Глубина рекурсии равна числу переменных - полиномиальна. На каждом уровне рекурсии нам нужно хранить лишь текущее значение переменной $y_i$ - один бит. Суммарная память $O(\text{poly}(n))$  
$\implies \mathbf{PH} \ni A \in \mathbf{PSPACE}$

**2. $\mathbf{PSPACE} \subset \mathbf{EXP}$:**  
Пусть $M$ - ДМТ с $S(n) = \text{poly}(n)$ памяти. Найдем максимальное количество возможных уникальных конфигураций этой машины:

- Состояний машины $|Q|$
- Позиций каретки на входной ленте $n$
- Позиций каретки на рабочей ленте $S(n)$
- Вариантов содержимого рабочей ленты $|\Gamma|^{S(n)}$

**Итого:**  
$$K = |\Gamma|^{S(n)} \cdot |Q| \cdot n \cdot S(n) = 2^{O(S(n))} = 2^{\text{poly}(n)}$$

Если машина работает дольше, чем $K$ шагов, она по принципу Дирихле повторит конфигурацию и зациклится $\implies$ корректная МТ обязана остановиться не более чем за $2^{\text{poly}(n)}$ шагов. Такое время работы по определению является экспоненциальным  
$\implies \mathbf{PSPACE} \subset \mathbf{EXP}$

---

# 17. Вывод $\mathbf{PSPACE} = \mathbf{NPSPACE}$ из теоремы Сэвича

> **Теорема Сэвича**  
> $S(n) \ge \log n \implies \mathbf{NSPACE}(S(n)) \subset \mathbf{DSPACE}(S^2(n))$  
> 
> $\implies \mathbf{NPSPACE} = \mathbf{PSPACE}$

$\triangle$  
**1. Суть теоремы Сэвича:**  
Распознавание языка на НМТ эквивалентно поиску пути в ориентированном графе конфигураций машины от стартовой конфигурации к принимающей. Число вершин - конфигураций:  
$K = |\Gamma|^{S(n)} \cdot |Q| \cdot n \cdot S(n) = O(2^{S(n)})$  
Определим рекурсивную функцию $\text{Reach}(u, v, k)$, которая проверяет, есть ли путь $u \to v$ длины $\le k$. Переход:

$$\text{Reach}(u, v, 2k) = \bigvee_x \text{Reach}(u, x, k) \land \text{Reach}(x, v, k)$$
$$\text{Reach}(u, v, 1) = (\text{u = v} \lor \text{ из u можно перейти в v за 1 шаг НМТ})$$
$\implies$ достижимость равносильна $\text{Reach}(u, v, K)$
Глубина рекурсии равна $O(S(n))$, на каждом уровне храним только текущую конфигурацию $w$, она требует $O(S(n))$ памяти  
Итого: $O(S^2(n))$

**2. Вывод $\mathbf{PSPACE} = \mathbf{NPSPACE}$:**

$\mathbf{PSPACE} \subset \mathbf{NPSPACE}$:  
Любая ДМТ - частный случай НМТ

$\mathbf{NPSPACE} \subset \mathbf{PSPACE}$:  
Пусть $L \in \mathbf{NPSPACE}$. По определению существует полином $p(n)$, что $L \in \mathbf{NSPACE}(p(n))$. Применим теорему Сэвича:

$$L \in \mathbf{NSPACE}(p(n)) \subset \mathbf{DSPACE}((p(n))^2)$$

Квадрат полинома тоже полином $\implies L \in \mathbf{PSPACE}$

---

# 18. Принадлежность к $\mathbf{PSPACE}$ задачи о булевых формулах с кванторами и задачи о выигрышной стратегии в обобщенной игре в города

> $\text{TQBF} \in \mathbf{PSPACE}$  
> $\text{GG} \in \mathbf{PSPACE}$

где:  
$\text{TQBF} = \{\varphi \mid Q_1 x_1 Q_2 x_2 \dots \varphi(x_1, \dots, x_k) = 1\}$  
$\text{GG}$ - игра на ориентированном графе, где два игрока по очереди двигают фишку, не посещая уже пройденные вершины. Выигрывает тот, кто делает последний возможный ход

$\triangle$

Общая идея - рекурсия + переиспользование памяти при возврате

**1. $\text{TQBF} \in \mathbf{PSPACE}$:**

- Если все переменные уже имеют значения, просто вычисляем значение формулы $\varphi$ ($O(|\varphi|)$ памяти)
- Если формула имеет вид $\exists x \ \Psi(x)$, рекурсивно вызываем от $\Psi(0)$ и $\Psi(1)$. Возвращаем их логическое ИЛИ
- Если формула имеет вид $\forall x \ \Psi(x)$, аналогично вызываем для $0$ и $1$, возвращаем логическое И

*Память:* $O(n)$ - полином  
$\implies \text{TQBF} \in \mathbf{PSPACE}$

**2. $\text{GG} \in \mathbf{PSPACE}$:**

Пусть $\text{Win}(v, \text{Visited})$ возвращает $1$, если из вершины $v$ есть выигрышная стратегия, где $\text{Visited}$ - массив флагов для уже посещенных вершин

- Перебираем исходящие из $v$ ребра
- Если $u \notin \text{Visited}$, помечаем $u$ как посещенную и рекурсивно вызываем $\text{Win}(u, \text{Visited})$
- Если $\text{Win}(u, \text{Visited})$ вернул $0$ (т.е. следующий игрок проигрывает), значит наш ход в $u$ - выигрышный, возвращаем $1$
- Перед переходом к следующему соседу снимаем пометку с $u$
- Если ни один ход не принес победу, возвращаем $0$

*Память:* Глубина рекурсии не превосходит числа вершин. Нужно хранить глобальный массив $\text{Visited}$ и на каждом уровне рекурсии хранить указатель на текущую вершину (размер $\log n$ бит). Итого $O(n \log n)$ - полином  
$\implies \text{GG} \in \mathbf{PSPACE}$

---

# 19. $\mathbf{L} \subset \mathbf{P}$, $\mathbf{NL} \subset \mathbf{P}$

> $$\mathbf{L} \subset \mathbf{P}$$
> $$\mathbf{NL} \subset \mathbf{P}$$

$\triangle$

Общее число конфигураций ДМТ/НМТ:
$$K = |Q| \cdot n \cdot S(n) \cdot |\Gamma|^{S(n)}$$
Так как память логарифмическая, то $S(n) = O(\log n) \implies |\Gamma|^{S(n)} \le |\Gamma|^{c \log n} = n^{c \log |\Gamma|} = \text{poly}(n)$ *($c \log |\Gamma|$ - константа)* $\implies$ общее число конфигураций - полином

**1. $\mathbf{L} \subset \mathbf{P}$:**  
Для ДМТ за каждым шагом строго следует один следующий. Если машина работает дольше, чем $K$ шагов, она зациклится. Поэтому полиномиальный алгоритм для задачи из $\mathbf{L}$ просто симулирует ДМТ не более $K$ шагов. Время симуляции полином  
$\implies \mathbf{L} \subset \mathbf{P}$

**2. $\mathbf{NL} \subset \mathbf{P}$:**  
Для НМТ вычисление сводится к поиску пути в ориентированном графе конфигураций. Поиск в графе работает за полином от количества вершин: $O(|V| + |E|) = O(K) = \text{poly}(n)$ *(конкретно в графе конфигураций МТ, исходящие степени вершин ограничены константой, поэтому не раздувается до $O(K^2)$. Хотя это тоже полином)*  
$\implies \mathbf{NL} \subset \mathbf{P}$


---

# 20. Принадлежность к $\mathbf{L}$ задач о сравнении и сложении двоичных чисел, о палиндроме, о сбалансированной последовательности скобок одного вида

> Задачи сравнения, сложения двоичных чисел, палиндрома и проверки скобочной последовательности лежат в классе $\mathbf{L}$

$\triangle$

Во всех задачах входное слово длины $n$ лежит на $\text{read-only}$ ленте. Нужно показать алгоритм, которому для работы на рабочих лентах требуется хранить только переменные, значения которых не превосходят $\text{poly}(n)$. Запись таких переменных требует $O(\log n)$ битов памяти

**1. Сравнение чисел**
- Числа записаны на одной ленте как `[  a  ]#[   b   ]`. Храним счетчик, и бегаем туда сюда, сравнивая разряды от старшего до младшего, увеличивая счетчик

**2. Сложение**
- Просто сложение в столбик. Нужен указатель на текущий разряд и переменная для переноса в следующий разряд. Оба принимают значения $O(n)$

**3. Палиндром (PAL): $x = x^R$**
- Заводим два указателя на начало и конец. На каждом шаге сравниваем символы и сдвигаем ближе

**4. Скобочная последовательность одного вида: `()(())`**
- Заводим индекс для прохода по строке и счетчик баланса. Если баланс хоть раз стал $< 0$ - отвергаем. Если в конце баланс равен $0$ - принимаем

---

# 21. Принадлежность к $\mathbf{NL}$ задач о пути в ориентированном графе и о сильной связности

> $\text{PATH} \in \mathbf{NL}$  
> $\text{SCONNECTED} \in \mathbf{NL}$

где:  
$\text{PATH} = \{(G, s, t) \mid \text{в орграфе } G \text{ есть путь из } s \text{ в } t \}$  
$\text{SCONNECTED} = \{G \mid \text{орграф } G \text{ сильно связен} \}$

$\triangle$

**1. $\text{PATH} \in \mathbf{NL}$:**  
Используем определение $\mathbf{NL}$ через НМТ
- *Алгоритм:* Начинаем с вершины $s$. На каждом шаге находимся в какой-то вершине $u$. Недетерминированно угадываем следующую вершину $v$ и проверяем по входной ленте, существует ли ребро $(u, v)$. Если нет - отвергаем. Если достигли $t$ - принимаем. Заводим счетчик шагов: если сделали $n$ шагов и не дошли до $t$ - отвергаем, т.к. простой путь не может быть длиннее $n$
- *Память:* Храним текущую вершину $u$ и счетчик шагов. Итоговая рабочая память $O(\log n)$

**2. $\text{SCONNECTED} \in \mathbf{NL}$:**  

Рассмотрим язык $\overline{\text{SCONNECTED}}$ - граф НЕ является сильно связным. НМТ угадывает две вершины, индексы вершин занимают $O(\log n)$ памяти. Теперь нужно проверить, что между ними нет пути - это задача $\text{coPATH} \in \mathbf{coNL} = \mathbf{NL}$ по Т. Иммермана-Селепченьи $\implies$ $\overline{\text{SCONNECTED}} \in \mathbf{NL} \ni \text{SCONNECTED}$

---

# 22. $\mathbf{P} \subsetneq \mathbf{P/poly}$

> $\mathbf{P}$ строго вложен в $\mathbf{P/poly}$

$\triangle$

**1. Вложение:**  
Если язык $L \in \mathbf{P}$, его распознает ДМТ $M$ за полиномиальное время $p(n)$. Работу такой машины можно развернуть в булеву схему. Таблица вычисления машины имеет размер $p(n) \times p(n)$ (строки - время, столбцы - ячейки ленты). Состояние каждой ячейки на шаге $t+1$ зависит только от $3$ ячеек над ней на шаге $t$ и состояния каретки. Эту локальную зависимость можно реализовать булевой схемой константного размера. Склеив $p(n)^2$ таких кусочков, получим схему размера $O(p(n)^2)$, которая симулирует $M$. Так как размер схемы полиномиален от $n$, $L \in \mathbf{P/poly}$

**2. Строгость вложения:**  

Рассмотрим любой унарный язык $U = \{1^n \mid n \in S\}$, где $S \subset \mathbb{N}$ - какое-то неразрешимое множество натуральных чисел. Для слов длины $n$ существует всего один возможный вход $1^n$. 
- Если $n \in S$, то в качестве схемы $C_n$ возьмем тривиальную схему, которая всегда выдает $1$.
- Если $n \notin S$, то схема $C_n$ всегда выдает $0$

Размер схемы $C_n$ равен $O(1) \implies U \in \mathbf{P/poly}$. Так как $S$ неразрешимо, $U \notin \mathbf{P}$

---

# 23. Иерархия схемной сложности: $\mathbf{NC}^d \subset \mathbf{AC}^d \subset \mathbf{NC}^{d+1}$, $\mathbf{NC}^0 \subsetneq \mathbf{AC}^0$, $\oplus \in \mathbf{NC}^1$

> $$\mathbf{NC}^d \subset \mathbf{AC}^d \subset \mathbf{NC}^{d+1}$$
> $$\mathbf{NC}^0 \subsetneq \mathbf{AC}^0$$
> $$\oplus \in \mathbf{NC}^1$$

$\triangle$

**1. Доказательство $\mathbf{NC}^d \subset \mathbf{AC}^d$:**  
По определению

**2. Доказательство $\mathbf{AC}^d \subset \mathbf{NC}^{d+1}$:**  
Пусть есть схема из $\mathbf{AC}^d$. В ней есть вентили с неограниченным числом входов. Общий размер схемы ограничен полиномом $\text{poly}(n)$, то и число входов в любой конкретный вентиль $k \le \text{poly}(n)$. Превращаем вентиль с $k$ входами в схему с вентилями по $2$ входа, строя двоичное дерево. Глубина такого дерева будет равна $\lceil \log_2 k \rceil$. Т.к. $k = \text{poly}(n) \implies \log k = O(\log n)$.  
Тогда исходная глубина схемы $O(\log^d n)$ умножается на глубину дерева $O(\log n)$, что по определению $\mathbf{NC}^{d+1}$:

$$ O(\log^d n) \times O(\log n) = O(\log^{d+1} n) $$

**3. Доказательство $\mathbf{NC}^0 \subsetneq \mathbf{AC}^0$:**  
$\mathbf{NC}^0$ означает, что глубина схемы ограничена $O(\log^0 n) = O(1)$ - константой $c$, а входы вентилей равны $2$. Значит выход схемы может зависеть максимум от $2^c$ входных битов - тоже константа. Схема из $\mathbf{NC}^0$ физически не сможет прочитать весь вход, если $n > 2^c$, а схема из $\mathbf{AC}^0$ может $\implies$ вложение строгое

**4. Доказательство $\oplus \in \mathbf{NC}^1$:**  
Функция $\oplus$ четность суммы $n$ битов: $x_1 \oplus x_2 \oplus \dots \oplus x_n$  
Операция $\oplus$ выражается как $(a \land \neg b) \lor (\neg a \land b)$ - константная глубина. Чтобы вычислить $\text{XOR}$ от $n$ битов, строим двоичное дерево из двувходовых вентилей $\oplus$. У этого дерева $n$ листьев, поэтому его глубина равна в точности $\log_2 n = O(\log^1 n)$. Размер схемы равен $n-1 = \text{poly}(n) \implies$ функция четности лежит в $\mathbf{NC}^1$

---

# 24. $\mathbf{RP} \subset \mathbf{BPP} \subset \mathbf{PP} \subset \mathbf{PSPACE}$, $\mathbf{RP} \subset \mathbf{NP}$

> $$\mathbf{RP} \subset \mathbf{BPP} \subset \mathbf{PP} \subset \mathbf{PSPACE}, \mathbf{RP} \subset \mathbf{NP}$$

$\triangle$

**1. $\mathbf{RP} \subset \mathbf{BPP}$:**  
Запустим алгоритм из $\mathbf{RP}$ $2$ раза. Вернем коньюнкцию результата  
Для $x \notin A$ вероятность ошибки останется $0 \le 1/3$  
Для $x \in A$ вероятность найти правильный ответ станет $\ge 1 - (\frac{1}{2})^2 = \frac{3}{4} \ge \frac{2}{3}$

**2. $\mathbf{BPP} \subset \mathbf{PP}$:**  
Очевидно из определений

**3. $\mathbf{PP} \subset \mathbf{PSPACE}$:**  
Детерминированно перебираем все $2^{p(n)}$ веток случайных битов, храня счетчик успешных веток (он занимает $p(n)$ бит, как и каждый конкретный $r$). Верификатор сам используем не больше полинома, и мы переиспользуем память на каждом запуске. $p(n) + p(n) + p(n) = \text{poly}(n)$. В конце просто сравниваем счетчик с $2^{p(n)-1}$ по определению $\mathbf{RP}$

**4. $\mathbf{RP} \subset \mathbf{NP}$:**  
В $\mathbf{RP}$ для $x \in A$ вероятность успеха $\ge \frac{1}{2}$, то есть существует хотя бы одна ветвь вычислений, ведущая к принятию. Это в точности определение серфиката $\mathbf{NP}$. Для $x \notin A$ вероятность равна $0$, то есть ни один сертификат не подходит. Значит, $\mathbf{RP}$ -  частный случай $\mathbf{NP}$

---

# 25. $\mathbf{IP} \subset \mathbf{PSPACE}$ для упрощённого определения $\mathbf{IP}$ со свежими случайными битами в каждом раунде

> $$\mathbf{IP} \subset \mathbf{PSPACE}$$

$\triangle$

По определению, язык $L \in \mathbf{IP}$, если существует полиномиальный по времени Верификатор $V$, Прувер $P$ обладает безграничной вычислительной мощностью и пытается максимизировать $\mathbb{P}[V^P(x) = 1]$.  
Наша задача: показать, что алгоритм, ограниченный полиномиальной памятью $\mathbf{PSPACE}$, может вычислить точную максимальную вероятность того, что $V$ примет слово $x$, а затем проверить, больше ли она $\frac{2}{3}$

Диалог между $P$ и $V$ можно представить как огромное **игровое дерево**:
- Корень дерева - начало протокола
- Глубина дерева ограничена числом раундов $k = \text{poly}(n)$
- В узлах, где ходит Верификатор, происходит разветвление по случайным битам $r$. Вероятность оказаться в конкретной ветке равна $\frac{1}{2^{|r|}}$
- В узлах, где отвечает Прувер, происходит разветвление по всем возможным сообщениям $m$. Так как Прувер играет оптимально, он выберет ветку, которая дает максимальную вероятность успеха
- В листьях дерева Верификатор запускает свой полиномиальный алгоритм и выдает $1$ или $0$

Алгоритм из $\mathbf{PSPACE}$ будет делать рекурсивный обход этого дерева в глубину, вычисляя функцию $\mathbb{P}(\text{узел})$ - максимальную вероятность принятия из этого узла:

1. **Если узел - конец диалога:**  
  Алгоритм запускает симуляцию Верификатора $V(x, \text{весь диалог})$. Это требует полиномиального времени, а значит и $O(\text{poly}(n))$ памяти. Возвращает $1$ или $0$
2. **Если узел - ход Верификатора:**  
  Алгоритм перебирает все возможные $r$, рекурсивно вычисляет вероятности для потомков и берет их среднее арифметическое **(так как биты равновероятны)**:  
  $$\mathbb{P}(\text{узел}) = \frac{1}{2^{|r|}} \sum \mathbb{P}(\text{потомок}_r)$$
3. **Если узел - ход Прувера:**  
  Алгоритм перебирает все возможные сообщения $m$ полиномиальной длины, рекурсивно вычисляет вероятности для потомков и берет максимум, так как Прувер ищет оптимальную стратегию:
  $$\mathbb{P}(\text{узел}) = \max_m \mathbb{P}(\text{потомок}_m)$$

**Анализ памяти:**  
Храним в памяти только путь от корня до текущего узла: 
- Длина пути равна числу раундов $O(\text{poly}(n))$
- На каждом уровне рекурсии мы храним текущее сообщение $m$ или случайные биты $r$. Их длины ограничены полиномом $O(\text{poly}(n))$, так как Верификатор работает за полиномиальное время и не может прочитать больше

**Итого:**  
(Глубина рекурсии) $\times$ (Память на один уровень) = $\text{poly}(n) \times \text{poly}(n) = \text{poly}(n)$

Мы можем вычислить максимальную вероятность принятия, используя $\text{poly}(n)$ памяти, мы просто проверяем, $\ge \frac{2}{3}$ ли она. Если да - слово принадлежит языку, Если нет - не принадлежит  
$\implies$ любой язык из $\mathbf{IP}$ можно распознать в $\mathbf{PSPACE}$

---

# 26. $\mathbf{PZK} \subset \mathbf{SZK} \subset \mathbf{CZK}$

> $$\mathbf{PZK} \subset \mathbf{SZK} \subset \mathbf{CZK}$$

$\triangle$

1. **$\mathbf{PZK} \subset \mathbf{SZK}$:**  
  В классе $\mathbf{PZK}$ распределение симулятора полностью совпадает с реальным распределением. Следовательно, статистическое расстояние между ними $0$  
  По определению $\mathbf{SZK}$, требуется, чтобы статистическое расстояние было $< \frac{1}{\text{poly}(n)}$. Значит, любой протокол из $\mathbf{PZK}$ автоматически удовлетворяет условиям $\mathbf{SZK}$

2. **$\mathbf{SZK} \subset \mathbf{CZK}$:**  
  Рассмотрим $S = \{\omega \mid D(x, \omega) = 1 \}$. По определению $\mathbf{SZK}$, для этого $S$, как и для любого другого $S' \subset \Omega$, верно $< 1/p(|x|)$. Определение $\mathbf{CZK}$ выполнено
